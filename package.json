{
  "name": "v2ray-config-bot",
  "version": "1.0.0",
  "description": "Telegram bot deployed on Cloudflare Workers",
  "main": "src/worker.js",
  "scripts": {
    "deploy": "wrangler publish"
  },
  "devDependencies": {
    "wrangler": "^4.17.0"
  },
  "dependencies": {
    // tambahkan dependencies bot kamu jika ada, misal:
    // "telegraf": "^4.12.0"
  },
  "engines": {
    "node": ">=16"
  }
}
